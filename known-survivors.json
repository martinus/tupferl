{
 "01b8158a62d9beb8": {
  "seen": 1,
  "why": "equivalent observably: this decides how finely the classes are split across batches. Every class is still placed exactly once -- `test_every_class_is_placed_exactly_once` -- and every test still runs."
 },
 "02b113cfe646dc93": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutants.py:1022 in _unquote() -- the `.endswith(...)` filter accepts everything. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "042c1f73a47d1770": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:975 in _run() -- the `if` is never taken. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "04630dbe22cc6996": {
  "seen": 1,
  "why": "equivalent: the guard is an early-out for the common case, and `any([])` is `False` -- the same answer the body computes from an empty pattern list."
 },
 "084032cd80a4d9c4": {
  "seen": 1,
  "why": "equivalent, measured: with the guard skipped, an empty `run` still returns `True` from the comprehension -- `whole[0:0] == []` holds and the range is never empty. The guard says the answer plainly rather than deriving it."
 },
 "0866a80e13e71a90": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutants.py:207 in _trimmed() -- `1` becomes `2`. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "090b0ec0a68c945c": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:1599 in run() -- the call to `timings.update(...)` never happens. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "09d8b4c1ca35dab3": {
  "seen": 1,
  "why": "equivalent observably: batch granularity only."
 },
 "0bd657cae368eac8": {
  "seen": 1,
  "why": "the `first` prefix -- the remembered killer tried ahead of a row. Skipping it costs a longer run and cannot change a verdict: the same tests run afterwards as part of the selection. That is the whole design of the cache, and `Killers`/`Learned` have their own tests for what goes into it."
 },
 "0d9ceebbe4fd38ad": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:2686 in main() -- the `if` is never taken. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "0fc3c0e98da7c9b6": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:2529 in sweep() -- the call to `print(...)` never happens. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "10eafe703e8d0fca": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:1976 in Killers.__init__() -- `self.cost` is never assigned. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "113f831025500aec": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutants.py:848 in _chosen() -- the ordering is reversed. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "13f833bc49ae31f2": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:2095 in Killers.save() -- the `if` is always taken. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "1648c547658110d4": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutants.py:1174 in _statements() -- the `if` is never taken. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "16542c4b0162ecb4": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:1614 in run() -- the `if` is always taken. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "18e3565d84896f9b": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:1615 in run() -- the call to `print(...)` never happens. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "19483ab35195c605": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:2701 in main() -- the `if` is always taken. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "194d55cc936f2cff": {
  "seen": 1,
  "why": "same as the `sorted` mutation on this line."
 },
 "1b9e22f48fdb3009": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:2250 in _bytes() -- the `if` is never taken. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "1dcd83b13d10b4c5": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutants.py:207 in _trimmed() -- the slice takes everything. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "1fd96f5a2418f577": {
  "seen": 1,
  "why": "the glob feeds two dicts keyed by module name; nothing downstream reads the order they were filled in. Sorted for a reproducible *build* of the index rather than for a consumer."
 },
 "1fe0d13152613728": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:2693 in main() -- the `if` is never taken. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "1fe7a4f1c3fab87f": {
  "seen": 1,
  "why": "equivalent: the mutation is on `left.get(key, 0)`'s *default*, and a default of -1 is still not `> 0`, so a key that was never accepted is still fresh."
 },
 "210d3deabb7df78f": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:588 in _from_proc() -- the `if` is never taken. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "21fc9f6cce96abe4": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/watch.py:273 in Watch.stalling() -- `<` becomes `<=`. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "22ca917a9b621dff": {
  "seen": 1,
  "why": "unreachable in CI and locally, both of which run as root: root ignores the mode bits, so no directory is unwritable. `test_doctor.TestTheBackupCheck.test_a_directory_that_exists_and_cannot_be_written_fails` is the test, and it skips itself with that reason."
 },
 "22da8c151d875b75": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutants.py:652 in slice_widened.widen() -- `clone.slice` is never assigned. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "2325c1be9fb3fca6": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:1534 in run() -- the `if` is never taken. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "236e89b65fd68b45": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutants.py:1059 in changed_lines() -- the `if` is never taken. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "240e88ea259ae2b4": {
  "seen": 2,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:1281 in _unclaimed() -- returns `None` instead of `0`. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "2508e6a527f0195e": {
  "seen": 1,
  "why": "the belt to `_end_lane`'s braces: if the session is somehow gone, at least reap the probe. Both halves aim SIGKILL at real processes, so neither is drivable from a test in this suite."
 },
 "256abcf3296477b2": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:2039 in Killers.ahead_of() -- the call to `print(...)` never happens. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "2591b9194f78e633": {
  "seen": 1,
  "why": "equivalent: with nothing remembered the comprehension below iterates an empty list and `' '.join([])` is the same empty string the guard returns. The guard says it plainly rather than deriving it."
 },
 "25ecdd5a4618c41b": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:2500 in sweep() -- the call to `print(...)` never happens. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "283e958f0d68da78": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:2680 in main() -- the `if` is never taken. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "2860d5d982e1af5c": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:1714 in run() -- the `if` is always taken. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "2a58a3fd129e77cf": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutants.py:1022 in _unquote() -- the `.startswith(...)` filter accepts everything. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "2b5311929c8c2044": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutants.py:1022 in _unquote() -- `and` becomes `or`. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "2c492948bea0bd24": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:2041 in Killers.ahead_of() -- the `if` is never taken. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "2cbcd3cfaec93aea": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutants.py:668 in drop_assign() -- the `if` is never taken. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "2d190f6ca745f031": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutants.py:974 in check() -- the `if` is always taken. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "2d2087820b75869b": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutants.py:1146 in _imported() -- the `if` is never taken. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "2f7b0b9ed4a70b1c": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutants.py:958 in _near_miss() -- `<` becomes `<=`. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "2f9bd64e30ba3441": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutants.py:207 in _trimmed() -- `-` becomes `+`. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "334b50321a2d87d8": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:1715 in run() -- the call to `_summarise(...)` never happens. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "3529c55411a5cadc": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:1721 in run() -- the call to `_RUNS.append(...)` never happens. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "3616a5b62d062e5e": {
  "seen": 1,
  "why": "unreachable within one run: `self.where` is set once per `Backups`, so the directory is created once. Two runs in the same second would collide, and no test can produce that without controlling the clock."
 },
 "3751adcd77f7bb35": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/watch.py:273 in Watch.stalling() -- `<` becomes `<=`. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "38181b00621d37d1": {
  "seen": 1,
  "why": "named in the code: `distance`'s own comment calls this an equivalent mutant and says why -- a git that would not compare produces output the parse below rejects anyway, reaching the same `None`."
 },
 "394911e732a4546e": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:2179 in generated() -- `share[row.path]` is never assigned. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "399c871815fa8102": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutants.py:207 in _trimmed() -- `+` becomes `-`. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "3ac19e49db539330": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:1714 in run() -- the `if` is never taken. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "3b0941e0cfdee309": {
  "seen": 1,
  "why": "`main` is called as `raise SystemExit(main())`, and `SystemExit(None)` exits 0 exactly as `SystemExit(0)` does. Indistinguishable from outside the process."
 },
 "3da34923c7b3d759": {
  "seen": 1,
  "why": "unreachable without breaking git mid-call: the branch fires when git lists a stage and then will not produce it. `held` is called with modes git has just reported, so producing the state means killing git between two of its own calls."
 },
 "3e09f9a12d6fbea5": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:1589 in run() -- the `if` is never taken. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "3f0da11f95f03a2b": {
  "seen": 1,
  "why": "unreachable: `init` refuses a repository that already exists, so the config file's parent is always being created for the first time. The flag is there for the shape of the call rather than for a case this code can meet."
 },
 "41824fddf2c80ff0": {
  "seen": 1,
  "why": "equivalent: `default=0` is used only when `found` is empty, and an empty `found` produces no lines for the width to pad. Measured: `'ab'.ljust(-1)` and `'ab'.ljust(0)` are both `'ab'`."
 },
 "42c58bfaa283b1ec": {
  "seen": 1,
  "why": "same branch as `verdict.py:428`, and the caller reads an absent report as `broke` either way."
 },
 "4310c1dd349bcf5c": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:1546 in run() -- `2` becomes `3`. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "436665c725ad08fe": {
  "seen": 1,
  "why": "equivalent given `_git`: the `rev-parse --verify` is an *early* check, and a bad ref fails again at the `git diff --merge-base` below, which raises through the same guard. What it buys is the message naming the ref rather than the diff invocation -- worth keeping, not separately observable."
 },
 "442c917f859b75fa": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:968 in _run() -- the `if` is never taken. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "45a3cb6536a359ba": {
  "seen": 1,
  "why": "equivalent observably: batch granularity only."
 },
 "46fd982d27654b8a": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutants.py:1177 in _statements() -- the call to `stack.extend(...)` never happens. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "48f7536d1c962056": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:1635 in run() -- the `if` is always taken. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "4b428357058183dc": {
  "seen": 1,
  "why": "same as the `sorted` mutation on this line: `by_size` re-orders the table afterwards."
 },
 "4bf66940ed0b3189": {
  "seen": 4,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:2517 in sweep.finished() -- the `if` is always taken. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "4d1cb3c9af05117b": {
  "seen": 1,
  "why": "unreachable through `pack`, which places each class in exactly one batch -- so no test id can be reported twice and `len(ran) - len(set(ran))` is always 0. The guard is there for a future batching rule that overlapped, which is the change it exists to catch."
 },
 "4d409dea34da2fd5": {
  "seen": 3,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutants.py:207 in _trimmed() -- `1` becomes `0`. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "4e46aeadadbdefb5": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:1714 in run() -- `and` becomes `or`. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "4f4a4f66a3e81888": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:2331 in _persist() -- the call to `print(...)` never happens. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "52ec3930250ffa26": {
  "seen": 1,
  "why": "a real gap, and a narrow one: forcing the branch sends a *file* to `manifest.collect`, which walks it and yields nothing, so `add` on a single file would admit nothing at all. Nothing distinguishes it because every add test that passes a file also passes through `check`, which already recorded the name. Worth a test; it needs `collect`'s behaviour on a non-directory pinned first, which is a change to `manifest` rather than to a fixture."
 },
 "53adeced17891419": {
  "seen": 1,
  "why": "equivalent, and the reason is an invariant worth stating: every rule carries the blob to the side that does *not* already hold it -- `TO_REPO` carries `home`, `TO_HOME` carries `stored` -- so writing it to the other side is a no-op and `write` reports no change. The backup is gated on `found != outcome.blob`, which is false in exactly those cases."
 },
 "5466a73933169eb5": {
  "seen": 1,
  "why": "same as `run_tests.py:424` -- the line only runs when a test id was reported twice, which `pack` makes impossible today."
 },
 "55bd999b89cdcc77": {
  "seen": 1,
  "why": "same as `doctor.py:187` -- the line only runs for an unwritable directory, which root does not have. Its test exists and skips."
 },
 "55d5b40bade7e38a": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutants.py:898 in generate() -- the `if` is never taken. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "5729e34124198089": {
  "seen": 1,
  "why": "equivalent for anything anyone reads: `TextTestRunner`'s verbosity controls the per-test detail on stderr, and the `Ran N tests` line that `test_the_worker_says_how_many_tests_it_ran` asserts is printed at every level."
 },
 "57f944faa307b9c2": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutants.py:405 in return_constant() -- the `not` is dropped. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "596c3933ada77648": {
  "seen": 1,
  "why": "named in the code: `keep_both`'s own comment says this branch is not reachable through the prompt, which offers `[b]` only for a text file, and reports rather than asserts the two ways it could happen."
 },
 "59f4a3c9fd7dfe54": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:2041 in Killers.ahead_of() -- the `if` is always taken. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "5cd26e2cd1fcbc95": {
  "seen": 1,
  "why": "equivalent: the split yields two parts, where index 1 and index -1 are the same element."
 },
 "5f10e5366ea11a6b": {
  "seen": 1,
  "why": "equivalent through the caller: the table this builds is re-ordered by `by_size` before anything runs it, so the order files are visited in here does not survive. `test_the_generated_table_itself_comes_back_smallest_first` asserts the order that does."
 },
 "5fa2b4870b70b1ea": {
  "seen": 1,
  "why": "equivalent, same argument: no real span is smaller than (0, 1) either, since a span's end always exceeds its start."
 },
 "6159c53f3e81f0e1": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutants.py:900 in generate() -- the call to `seen.add(...)` never happens. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "61b58c8f0dd78f21": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutants.py:1024 in _unquote() -- the `.startswith(...)` filter accepts everything. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "648458c4486cee47": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutants.py:316 in connector.flip() -- `clone.op` is never assigned. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "655cbb96b5a952ca": {
  "seen": 1,
  "why": "`rglob` order is the filesystem's, so `list` gives whatever order the directory happens to hold. The result is a dict keyed by path and every caller sorts it again -- `generated` iterates `sorted(touched)` and `by_size` re-orders the table -- so the walk order does not survive to anything observable."
 },
 "65808a2ac70ba16f": {
  "seen": 1,
  "why": "cosmetic, same as the `0` mutation on this line."
 },
 "66149fff98eb7a7e": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:2721 in main() -- the call to `_pidfile(args.json).write_text(...)` never happens. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "6682fe441231f0e9": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:637 in _parse_ps() -- `all` becomes `any`. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "67472e44bc956363": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:2158 in generated() -- `==` becomes `!=`. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "68e120dc2434922e": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:2479 in sweep() -- `sorted` becomes `list`. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "699cf3c77229704f": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:1546 in run() -- `*` becomes `//`. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "69b00a467b703114": {
  "seen": 1,
  "why": "unmeasurable by construction: forcing this branch off makes every spawned worker ignore `--worker`, run the whole suite itself and spawn more -- an unbounded fork storm. `_Lanes` killed the session at 2199 MiB against its 2063 MiB share and took the rest of the table with it, so the row can never come back anything but BROKE."
 },
 "69f2b7dd073ba2ce": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutants.py:1176 in _statements() -- the `if` is never taken. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "6e8aca54cf249e0f": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutants.py:1149 in _imported() -- the `if` is always taken. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "6f3634f8f69514cf": {
  "seen": 1,
  "why": "equivalent observably on any machine with at least two usable CPUs: the factor decides how many batches run at once, not what they run. (On a single-CPU machine `usable_cpus() // 2` would be 0 and `ThreadPoolExecutor(max_workers=0)` would raise -- not reachable here, and named rather than guarded.)"
 },
 "712a7c8c0d56650c": {
  "seen": 1,
  "why": "same as `mutate.py:702`: the escapee pass, sending SIGKILL to processes outside the group. Untestable for the same reason and covered by `_lane`'s tests, which decide the list."
 },
 "714a5e2faddfb392": {
  "seen": 1,
  "why": "equivalent: an exit status git sets also means empty stdout, and the parse below turns that into `{}` by itself -- the loop sees one empty field, `not raw` skips it, and the same empty dict comes back. The guard is an early-out, not a decision. Its neighbour on 514 (`return {}` becoming `return None`) *is* caught, by `TestWhenGitWillNotAnswerAboutConflicts`."
 },
 "7731bcc6aebba5e0": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutants.py:1174 in _statements() -- the `if` is always taken. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "77e25141b2553d05": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:945 in _run() -- the `if` is never taken. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "794f6f304ef0788f": {
  "seen": 1,
  "why": "equivalent: the default only orders a span-less row against spanned ones, and every real span starts at or after 0 with its end beyond its start -- so a default below (0, 0) sorts to the same place (0, 0) does."
 },
 "7b120329cf384ea2": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/watch.py:331 in _await_pid() -- `>=` becomes `>`. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "7b2933fa7e13c925": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:2179 in generated() -- `+` becomes `-`. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "7cf4f1fcbc5c92de": {
  "seen": 1,
  "why": "prints an empty heading above an empty list. `reached` is a reading tool whose output a person interprets, and its exit status -- the thing a script reads -- is asserted."
 },
 "7d22973be0651d07": {
  "seen": 1,
  "why": "a prefix naming a test that will not import. Reachable only from a killers cache written by an older tree, which `Killers` already validates once per run -- so by the time `collect` sees it, it has been checked."
 },
 "7fce2e3d1befea71": {
  "seen": 1,
  "why": "cosmetic, same as the `-1` mutation on this line."
 },
 "85c6fb65e7105fb9": {
  "seen": 1,
  "why": "`TextTestResult.startTestRun` is a hook with no body in the stdlib, and `Verdicts` does not override it. Nothing in the process has anything to do at that point."
 },
 "85e504364de36b5d": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:588 in _from_proc() -- `<` becomes `<=`. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "8646cb49a32ec321": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:1538 in run() -- the call to `check(...)` never happens. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "875b76487e97c890": {
  "seen": 1,
  "why": "equivalent: the code is read only through `Result.ok` (`code != 0`) and `gitrepo.reason`, neither of which distinguishes 1 from 2. Nothing in the package branches on a particular non-zero status."
 },
 "879d13fd2685d6ee": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:2038 in Killers.ahead_of() -- `-` becomes `+`. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "8845ba5590eed393": {
  "seen": 1,
  "why": "measured and named in the suite: `TestWhenOneSideReplacedTheFileWithASymlink`'s docstring records that git splits a type change into two paths of one stage each (`{'.config/thing': {3: 0o120000}, '.config/thing~HEAD': {2: 0o100644}}`), so a file with *mixed* stage kinds cannot be built and the two spellings cannot be told apart."
 },
 "896416326f77945e": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:2746 in main() -- the call to `_persist(...)` never happens. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "8a470f92576749fd": {
  "seen": 2,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:1149 in Learned.ahead() -- `any` becomes `all`. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "8ad88aa7ed4c72fa": {
  "seen": 1,
  "why": "equivalent, measured: `sys.version_info` on a real 3.11.0 is `(3, 11, 0, 'final', 0)`, which is already `> (3, 11)`. The two spellings can only differ for a version tuple of exactly `(3, 11)`, which no interpreter reports."
 },
 "8b52b775bfd2d5f6": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:2130 in generated() -- the `if` is never taken. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "8bbc30689a5e975c": {
  "seen": 1,
  "why": "cosmetic: the cache's indent. It is read back by `json.loads`, and `test_it_writes_a_file_a_later_run_can_read` asserts that round trip."
 },
 "91685883a672cb30": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:2038 in Killers.ahead_of() -- the `if` is never taken. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "92381fafce5cf356": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:1600 in run() -- `!=` becomes `==`. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "9343ceda67f321cc": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:2180 in generated() -- the ordering is reversed. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "9344ccb85e1620a9": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:1967 in Killers.__init__() -- the `if` is always taken. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "9352db626467d7fd": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutants.py:1139 in _imported() -- the `if` is never taken. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "9476ceeca43ea8cc": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutants.py:1111 in every_line() -- the `if` is always taken. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "95d4f3ffe0229d8e": {
  "seen": 1,
  "why": "the teardown, and no test may drive it: it sends SIGKILL to a real process group, and a fixture that got the group wrong would kill the suite running it. Guarded instead by `_lane`, whose membership walk is what decides *which* group -- that is where the defect found in #58 was, and it has tests."
 },
 "96b7cd757a67efc0": {
  "seen": 1,
  "why": "the rewrite that turns `min` into `max` and `any` into `all`. Dropping it leaves the call unchanged, so the generated 'mutation' is a no-op row that every suite reports as SURVIVED -- visible only as a sweep whose survivors are all of one operator, which no single verdict can show. Guarded indirectly: `TestTheOperators` asserts the text each operator produces."
 },
 "96c47c77ab98e25c": {
  "seen": 1,
  "why": "equivalent, same argument as the `2` mutation on this line."
 },
 "9e43f2186e3a5952": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:1714 in run() -- the `not` is dropped. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "9fd1c1c8c04a3130": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:2512 in sweep.finished() -- the call to `print(...)` never happens. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "a29b7b497dcb4316": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:573 in _from_proc() -- the `if` is never taken. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "a53382cc782c2f44": {
  "seen": 1,
  "why": "equivalent: one extra starting offset, whose slice is one element short and so cannot match. Same argument as the `-`/`+` mutant on this line."
 },
 "a66f87eb42164554": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutants.py:404 in return_constant() -- `is` becomes `is not`. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "a9efda6de2c726e7": {
  "seen": 1,
  "why": "equivalent: as above -- the default is reached only with nothing to pad, and `ljust` treats every width below the string's length alike."
 },
 "ab538f035ef97115": {
  "seen": 1,
  "why": "equivalent given git's contract: the two operands disagree only for a row that is non-empty *and* malformed, and `ls-files -u -z` always emits three fields. The trailing empty field after the final NUL satisfies both spellings. The guard is there so a future git cannot turn a merge into a `ValueError`, which is a claim about git rather than about this code."
 },
 "ad87a9574aadf2c8": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/watch.py:225 in Watch.__init__() -- `self.told` is never assigned. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "ada56a3acd458f19": {
  "seen": 1,
  "why": "equivalent: `args.operator or None` passes `None` when the list is empty, and `[] and None` is `[]` -- which `mutants.generate` treats identically, since it tests the argument for truth rather than for `None`."
 },
 "add7706de5b64979": {
  "seen": 1,
  "why": "same set, same argument as the `sorted` mutation on this line -- and reversal is equally invisible to a caller that only needs the names."
 },
 "aeebfa9078f9a8a4": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:2420 in _recorded() -- the `if` is never taken. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "b1b72d3ace0ebef8": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:1601 in run() -- the call to `print(...)` never happens. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "b2642549b1048426": {
  "seen": 1,
  "why": "equivalent: the guard selects which swap table entry applies, and skipping it yields no `Edit` for a name that has one -- a *missing* mutation rather than a wrong one. A sweep with fewer rows in it is not something a verdict can see; `test_mutants` asserts the operators that are produced, which is the direction that matters."
 },
 "b2dbd4344abf5ac4": {
  "seen": 1,
  "why": "unreachable through the harness: a `caught` verdict always carries a killer -- `verdict.py` records the test that noticed it in the same step that decides the outcome -- so the two operands cannot disagree. The guard is there for a hand-built `Result`, which is a test's shape rather than a run's."
 },
 "b4a4e7d8abb4aae6": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:2158 in generated() -- the `if` is always taken. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "b57e8f3bad31f051": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutants.py:848 in _chosen() -- `sorted` becomes `list`. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "b5c810f44ae53ee0": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:2159 in generated() -- the call to `print(...)` never happens. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "b65863cfef898578": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:2166 in generated() -- the call to `print(...)` never happens. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "b6b6067bda1f3d0a": {
  "seen": 2,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:2526 in sweep() -- the `if` is never taken. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "b6fe5577cb2aed90": {
  "seen": 1,
  "why": "equivalent, same argument as `mutate.py:2387` for `--skip-operator`."
 },
 "b70f9c717613cca9": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:2008 in Killers.prefix() -- `<` becomes `<=`. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "b76f804a441751c4": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:2136 in generated() -- the `if` is never taken. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "b8b4c604f9487a37": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutants.py:1175 in _statements() -- the call to `stack.append(...)` never happens. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "bb6a762363762b38": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutants.py:404 in return_constant() -- the `if` is always taken. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "bdb6ec52c393c0f3": {
  "seen": 1,
  "why": "equivalent through every caller: `integrate` truth-tests the result (`if not left`) and puts it in a message only when it is non-empty, so `None` and `[]` are the same answer. A type violation with no reader -- worth a note, not a fixture."
 },
 "c10a95c7433ae145": {
  "seen": 1,
  "why": "equivalent observably: `--jobs` defaults to a falsy 0 so that `args.jobs or usable_cpus() * 2` reaches the computed default. A default of 1 makes an unset flag mean one job -- slower, and the same tests with the same verdicts."
 },
 "c29843dc1f778b48": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:1589 in run() -- the `if` is always taken. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "c317fb21fc279d9f": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:2709 in main() -- the `if` is never taken. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "c39e4c578e3bfa77": {
  "seen": 1,
  "why": "equivalent, same invariant as `sync.py:792`: the blob is always the side the other one is missing, so the extra write changes no bytes and takes no backup."
 },
 "c5623c2f225cc753": {
  "seen": 1,
  "why": "equivalent: after a concluded merge git has cleared `MERGE_HEAD`, so the extra `git merge --abort` fails and its `Result` is discarded. The guard stops a *successful* merge being undone by its own `finally`, which is a claim about a git that behaved differently."
 },
 "c61404f06050ed83": {
  "seen": 1,
  "why": "the reap after the kill. Dropping it leaves a zombie rather than changing an answer, so nothing a verdict can see is different."
 },
 "c7d9c88ef63c2752": {
  "seen": 1,
  "why": "equivalent for what `discover` produces: `unloadable` is filled by a walk that visits modules in sorted order, so its insertion order already is the sorted order. The *reversal* of this line is caught by `test_broken_modules_are_named_in_a_settled_order`; only the redundancy is not."
 },
 "c83d589af7e9932e": {
  "seen": 1,
  "why": "a timeout on the reap: `_SAMPLE * 5` is 5 seconds, `_SAMPLE // 5` is 0. Both return promptly for a process that has just been SIGKILLed, and the difference is only visible for one that ignores it -- which SIGKILL cannot be."
 },
 "c8535c69832956cb": {
  "seen": 2,
  "why": "a test reaches this line and asserts nothing about it -- tools/watch.py:110 in tint() -- `1` becomes `2`. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "c88cebd305e6305d": {
  "seen": 1,
  "why": "the per-file counts come from a dict built in drop order, which for this tree already happens to be alphabetical -- so `list` and `sorted` agree. `test_a_capped_table_says_what_it_dropped_and_from_where` asserts the order, and would catch it on a tree where the two differed."
 },
 "c96d30b744843f4f": {
  "seen": 1,
  "why": "cosmetic: the verbosity of a `TextTestResult` writing into an `io.StringIO` nothing reads. The stream is there because the class requires one, not to be shown."
 },
 "cb48d09bfcf8269e": {
  "seen": 1,
  "why": "equivalent: with `base` `None` the two comparisons inside are `Blob == None`, which is `False` (measured), so the forced branch falls through to exactly the merge the unforced code reaches."
 },
 "ced7b751cbd406b2": {
  "seen": 1,
  "why": "equivalent: this sorts the *packages* the walk visits, and the function ends `return sorted(set(found))` -- so the order the names are appended in is not observable. The sort on line 346 *is* guarded, by `test_what_it_returns_is_sorted`."
 },
 "d02dec918447cea4": {
  "seen": 1,
  "why": "equivalent, and a test asserting otherwise was written and removed rather than shipped: `uses_helper` is only ever read against `helpers`, which holds the non-`test_` files under `tests/`, so an entry keyed by a product module is looked up and never found. The first attempt asserted `tupferl.widget` was absent from the index -- it is present, as a key mapping to both tests, which is the correct closure. The assertion could not fail."
 },
 "d3fe3bd36813eef2": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutants.py:912 in generate() -- `or` becomes `and`. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "d480c11af53967ff": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutants.py:370 in order.unordered() -- `clone.func` is never assigned. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "d529251f459680bd": {
  "seen": 1,
  "why": "the matching hook, equally empty. Both are called because the protocol says to, not because either does anything here."
 },
 "d52e7b86d922fe9f": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutants.py:1176 in _statements() -- the `if` is always taken. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "d5386cd6853d5e56": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutants.py:985 in check() -- the `if` is never taken. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "d543e67c0a673583": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:2479 in sweep() -- the ordering is reversed. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "d5b0a3a963bdb417": {
  "seen": 1,
  "why": "equivalent, same argument as the `.startswith` mutation on this line."
 },
 "db06fb0f844c123d": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:1304 in _confined() -- the `if` is never taken. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "db25bce2f34d37cd": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:2158 in generated() -- the `if` is never taken. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "dbc04bb253f7ad88": {
  "seen": 1,
  "why": "equivalent: the extra iterations slice past the end of `whole` and produce lists shorter than `run`, which cannot equal it. Measured: a run that is absent stays absent with the wider range."
 },
 "dbc07c68c46d145d": {
  "seen": 1,
  "why": "equivalent for the names git can produce, same argument as `gitrepo.py:474`: `conflicted`'s dict is built in git's own byte order. The user-facing ordering that this feeds is asserted by `TestWhenSeveralFilesCannotBeSettled`."
 },
 "dc376b70a2bca7c6": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:1614 in run() -- the `if` is never taken. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "df14f08837d1a8b7": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:2701 in main() -- the `if` is never taken. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "e315e92455834779": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/watch.py:333 in _await_pid() -- the call to `time.sleep(...)` never happens. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "e406577f873afdb9": {
  "seen": 1,
  "why": "equivalent, same argument: the package order feeds a result that is sorted again before it is returned."
 },
 "e512d739852fa03c": {
  "seen": 1,
  "why": "equivalent, same argument as the `1` mutation on this line: a uniform offset to every bin's starting weight."
 },
 "e68d36f8b8262855": {
  "seen": 1,
  "why": "equivalent in practice: `memory <= 0` becoming `<= 1` differs only for a cap of exactly one byte."
 },
 "ea734e1bb10b5e56": {
  "seen": 1,
  "why": "measured unreachable: `discover` drops a module it could not import from `classes` as well as recording it, so no batch is ever asked to load one and no batch report carries an `unloadable` entry. Verified by driving a real tree with a broken module -- exactly one `could not import` line is printed, from the parent's own discovery. The line guards a `discover` that stopped doing that."
 },
 "eacbe9005c394278": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutants.py:956 in _near_miss() -- `>` becomes `>=`. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "ebb49eb4e05dcb9d": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:2147 in generated() -- `or` becomes `and`. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "ebb68e0270733334": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutants.py:364 in order.backwards() -- `clone.keywords` is never assigned. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "ec909589daea00a8": {
  "seen": 1,
  "why": "equivalent: with nothing unloadable the loop over `loader.errors` prints nothing and `_loaded(suite, {})` filters nothing out, so the forced branch produces the same suite and the same output."
 },
 "ed76cb6b26830509": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:1661 in run() -- the call to `timings.update(...)` never happens. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "edc8761750cbdb95": {
  "seen": 1,
  "why": "the teardown path again -- see `mutate.py:702`. Reached only when a probe has to be killed, and a test that produced one would be killing processes beside the suite."
 },
 "ee93a88107751c87": {
  "seen": 1,
  "why": "unreachable: this is the fallback for a machine that reports *no* limit at all -- no cgroup file, no inherited budget, and `sysconf` unavailable. On any POSIX machine the `sysconf` arm always appends, so `limits` is never empty. The `limits.append` on the line above it *is* guarded, by `test_the_host_total_is_one_of_the_limits`."
 },
 "f1931326a8dc38a0": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:2043 in Killers.ahead_of() -- the call to `print(...)` never happens. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "f28511da0828272c": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:1600 in run() -- the `if` is always taken. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "f2ee24b28075cb95": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:2420 in _recorded() -- `or` becomes `and`. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "f3ca8786fa13ae9d": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:1589 in run() -- `is not` becomes `is`. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "f3da80e21dbf9b87": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:2709 in main() -- the `if` is always taken. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "f471a63f6a4a0d25": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:2737 in main() -- the call to `print(...)` never happens. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "f483a996fd14f7ec": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutants.py:371 in order.unordered() -- `clone.keywords` is never assigned. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "f559e7996a7d12dd": {
  "seen": 1,
  "why": "the values come from a set comprehension, so `list` gives hash order -- which Python randomises per run. Caught only when that order happens to differ from sorted, which makes it a coin flip rather than a guard. Left rather than pinned: the caller feeds the names to a shard selection, where order changes nothing."
 },
 "f8dc4f4f23d4a6ff": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:2646 in main() -- the call to `parser.add_argument(...)` never happens. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "f90788e320e05988": {
  "seen": 1,
  "why": "same as `verdict.py:425`: the prefix is an ordering optimisation, and dropping it changes when a killer is found rather than whether."
 },
 "faf2e1f470d95e57": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:2018 in Killers.prefix() -- `>` becomes `>=`. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "fc188d2b6cc3623a": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:1662 in run() -- the `if` is never taken. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "fcaf6ff1213c97b1": {
  "seen": 1,
  "why": "equivalent for the names git can produce: `conflicted` builds its dict from `ls-files -u -z`, which git emits in byte order, and UTF-8 byte order agrees with Python's code-point order. The *reversal* of this line is caught by `test_two_conflicted_files_come_back_in_a_settled_order`; only the redundancy of sorting an already-sorted list is not."
 },
 "fdf7c7970efd0316": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:2744 in main() -- the call to `killers.save(...)` never happens. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "fe295f336186c1ea": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutants.py:207 in _trimmed() -- `<=` becomes `<`. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "fedc0c99326e1ab8": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:883 in _run() -- the call to `_clear_bytecode(...)` never happens. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "ff550d58af766d96": {
  "seen": 1,
  "why": "a test reaches this line and asserts nothing about it -- tools/mutate.py:1599 in run() -- `or` becomes `and`. Weak fixture or equivalent; from the whole-tree sweep of 2026-08."
 },
 "ff5554e8685e5cac": {
  "seen": 1,
  "why": "equivalent, measured: `Path.cwd() / Path('/etc/x')` is `/etc/x` -- pathlib's `/` returns the right operand when it is absolute, so taking the branch for an already-absolute path is the identity."
 },
 "ffdd15a617c9f9e4": {
  "seen": 1,
  "why": "same as the `sorted` mutation on this line: the result is a dict its callers re-order."
 }
}
